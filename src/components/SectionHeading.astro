---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  href?: string;
  linkLabel?: string;
  id?: string;
}

const { eyebrow, title, description, href, linkLabel, id } = Astro.props;
---

<header class="section-heading">
  <div class="section-heading__copy">
    {eyebrow && <p class="section-heading__eyebrow">{eyebrow}</p>}
    <h2 id={id}>{title}</h2>
    {description && <p class="section-heading__description">{description}</p>}
  </div>
  {
    href && linkLabel && (
      <a class="section-heading__link" href={href}>
        {linkLabel}
        <span aria-hidden="true"> →</span>
      </a>
    )
  }
</header>

<style>
  .section-heading {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 1rem 2rem;
    align-items: end;
    padding-block-end: 1rem;
    border-block-end: 1px solid var(--color-rule, #c9c5ba);
  }

  .section-heading__copy > * {
    margin: 0;
  }

  .section-heading__eyebrow {
    margin-block-end: 0.375rem;
    color: var(--color-accent-dark, #8f2f20);
    font-family: "Source Sans 3 Variable", "Segoe UI", sans-serif;
    font-size: 0.875rem;
    font-weight: 750;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h2 {
    max-inline-size: 24ch;
    color: var(--color-ink, #171918);
    font-family: "Newsreader Variable", Georgia, serif;
    font-size: clamp(1.75rem, 1.35rem + 1.35vw, 2.75rem);
    letter-spacing: -0.025em;
    line-height: 1.02;
  }

  .section-heading__description {
    max-inline-size: 62ch;
    margin-block-start: 0.5rem;
    color: var(--color-soft-ink, #3f4544);
  }

  .section-heading__link {
    color: var(--color-accent-dark, #8f2f20);
    font-weight: 700;
    text-underline-offset: 0.2em;
  }

  @media (max-width: 35rem) {
    .section-heading {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }
</style>
