---
interface Props {
  businessProblem: string;
  technologyFocus: string;
  intendedAudience: string;
  deliverable: string;
}

const { businessProblem, technologyFocus, intendedAudience, deliverable } =
  Astro.props;
const fields = [
  ["Business problem", businessProblem],
  ["Technology focus", technologyFocus],
  ["Intended reader", intendedAudience],
  ["What you will produce", deliverable],
] as const;
---

<section class="fit-summary" aria-labelledby="fit-heading">
  <h2 id="fit-heading">At a glance</h2>
  <dl>
    {
      fields.map(([term, value]) => (
        <div>
          <dt>{term}</dt>
          <dd>{value}</dd>
        </div>
      ))
    }
  </dl>
</section>
